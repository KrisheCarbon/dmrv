import SectionOverview from "@/components/SectionOverview";

export default function OperationsOverviewPage() {
  return (
    <SectionOverview
      section="Operations"
      description="Trainings, inspections, intents, queries, and field updates."
    />
  );
}
