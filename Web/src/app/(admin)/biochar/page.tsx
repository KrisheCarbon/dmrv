import SectionOverview from "@/components/SectionOverview";

export default function BiocharOverviewPage() {
  return (
    <SectionOverview
      section="Biochar"
      description="Production runs, mixing, application, fuel, farms, and feedstock."
    />
  );
}
