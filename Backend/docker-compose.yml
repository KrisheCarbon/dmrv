services:
  backend:
    build:
      context: ..
      dockerfile: Backend/Dockerfile
    image: krishecarbon-backend:latest
    ports:
      - "${PORT:-3001}:3001"
    env_file:
      - .env
    environment:
      PORT: 3001
    restart: unless-stopped
